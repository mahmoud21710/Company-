@model IEnumerable<RoleViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>All Roles</h1>
<br />
<br />
<form asp-action="Index">
    <div class="row m-5">
        <div class="col-8">
            <input type="text" id="id" name="InputSearch" class="form-control" />
        </div>
        <div class="col-4">
            <input type="submit" name="Search" class="btn btn-success" />
        </div>
    </div>
</form>
<br />

<a asp-controller="Role" asp-action="Create" class="btn btn-primary">Create Role</a>
<br />
<br />
@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(U => U.Id)        </th>
                <th>@Html.DisplayNameFor(U => U.RoleName)        </th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.Id</td>
                    <td>@user.RoleName</td>
                    

                    <partial name="PartialViews/_ButtonPartialView" model="@user.Id">
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-danger">
        <h4>
            There are No Roles
        </h4>
    </div>
}

